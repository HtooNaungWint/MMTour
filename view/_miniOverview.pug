mixin overview(name, text, icon)
    .overview-box__detail
        svg.overview-box__icon
            use(xlink:href=`${icon}`)
        span.overview-box__label=name
        span.overview-box__text=text
